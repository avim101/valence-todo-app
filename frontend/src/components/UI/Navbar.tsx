import Title from "./typography/Title";

const Navbar = () => {
    return (
        <div className="navbar bg-primary text-primary-content justify-center">
            <Title level={3}>Valance Todo App</Title>
        </div>
    );
}

export default Navbar;
